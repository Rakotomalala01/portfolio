import { FC } from "react";

const Experience: FC = () => {
  return (
    <div className="flex items-center justify-center h-full text-white text-lg font-blackops">
      Experience Section (Coming Soon)
    </div>
  );
};

export default Experience;
